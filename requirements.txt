# =============================================================================
# Consumer Credit Rights Assistant — Python dependencies
# Python 3.11 required
#
# Install:
#   pip install -r requirements.txt
#
# Note: langchain-core is intentionally NOT pinned here — pip resolves the
# correct version automatically based on the langchain 1.x family constraints.
# =============================================================================


# -----------------------------------------------------------------------------
# [api] — FastAPI gateway layer
# -----------------------------------------------------------------------------
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.10
pydantic-settings==2.14.2
python-multipart==0.0.20
httpx==0.28.1
sse-starlette==2.3.6


# -----------------------------------------------------------------------------
# [ingestion] — PDF parsing  (Stage 1 ablation)
# -----------------------------------------------------------------------------
pypdf==5.5.0
pdfplumber==0.11.6
pymupdf==1.25.5

# Chunking (Stage 2) — LlamaIndex node parsers
llama-index-core==0.14.23
llama-index-embeddings-huggingface==0.7.0
llama-index-embeddings-openai==0.6.0
tiktoken==0.9.0
python-dotenv==1.2.2

# Sparse index — BM25 for hybrid search (Stage 5)
# BM25Encoder from pinecone-text produces the sparse_values format Pinecone
# expects. rank-bm25 is no longer used — pinecone-text replaces it.
pinecone-text==0.11.0

# Vector DB upsert (Stage 4)
pinecone==6.0.2
chromadb==0.6.3
faiss-cpu==1.10.0


# -----------------------------------------------------------------------------
# [embeddings] — Stage 3 ablation alternatives
# -----------------------------------------------------------------------------
sentence-transformers==3.4.1
openai==2.52.0


# -----------------------------------------------------------------------------
# [structured_data] — FRED CSV → SQLite trend tool
# -----------------------------------------------------------------------------
pandas==2.2.3
numpy==1.26.4


# -----------------------------------------------------------------------------
# [memory] — SQLite persistence layer
# -----------------------------------------------------------------------------
sqlalchemy==2.0.40
aiosqlite==0.21.0


# -----------------------------------------------------------------------------
# [orchestration] — LangChain 1.x family + multi-provider LLM clients
# langchain-core is resolved automatically; do NOT pin it explicitly.
# -----------------------------------------------------------------------------
langchain==1.3.14
langchain-community==0.4.2
langchain-openai==1.4.1
langchain-google-genai==4.3.2
langchain-anthropic==1.5.3
google-generativeai==0.8.4
anthropic==0.120.2


# -----------------------------------------------------------------------------
# [guardrails] — Policy file loading + rule checks
# -----------------------------------------------------------------------------
pyyaml==6.0.2
regex==2024.11.6


# -----------------------------------------------------------------------------
# [evaluation] — RAGAS, DeepEval, retrieval metrics
# -----------------------------------------------------------------------------
ragas==0.4.3
deepeval==4.1.5
datasets==4.8.5
nltk==3.9.3


# -----------------------------------------------------------------------------
# [dev] — Testing and linting
# -----------------------------------------------------------------------------
pytest==8.3.5
pytest-asyncio==0.26.0
ruff==0.11.9
